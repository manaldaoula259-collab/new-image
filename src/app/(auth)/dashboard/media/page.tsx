import DashboardMediaPage from "@/components/pages/DashboardMediaPage";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Media Library",
};

const DashboardMedia = () => <DashboardMediaPage />;

export default DashboardMedia;

